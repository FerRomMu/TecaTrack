# STT spike — PoC dependencies
# Tested with Python 3.12 (faster-whisper / ctranslate2 have no wheels for 3.14 yet).
# Create an isolated env, e.g. with uv:
#   uv venv --python 3.12 .venv
#   uv pip install -r requirements.txt
#   uv run --python .venv/bin/python jupyter nbconvert --to notebook --execute stt_poc.ipynb

faster-whisper==1.2.1   # self-hosted Whisper (CTranslate2 backend, CPU int8)
edge-tts==7.*           # synthetic es-AR samples (no API key, free)
jiwer==4.*              # WER / normalized WER metrics
soundfile==0.13.*       # audio inspection
jupyter
nbconvert
ipykernel

# Optional managed-API paths (only needed to run those cells):
#   groq            # Groq-hosted Whisper large-v3 / v3-turbo (free tier)
#   huggingface-hub # Hugging Face Inference Providers (free monthly credit)
